# AI Test-Case-to-Selenium Demo Web App

A lightweight web app that explains and demonstrates the concept of using an LLM as a translator from plain-English test cases to Selenium Java automation code.

## Run locally

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.
